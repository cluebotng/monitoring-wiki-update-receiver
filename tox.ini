[tox]
requires = tox>=4
envlist = ruff,black

[testenv]
package = skip
use_current_env = true

[testenv:black]
commands = black --check {posargs:monitoring_wiki_receiver}
allowlist_externals = black

[testenv:ruff]
commands = ruff check {posargs:monitoring_wiki_receiver}
allowlist_externals = ruff
